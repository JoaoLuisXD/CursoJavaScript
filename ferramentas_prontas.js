localStorage.setItem("nome","Joao"); //define um item no localStorage.

setTimeout(() => {
    alert("oi");
},3000); //define um tempo até aparecer o alerta na tela.

setInterval(() => {
    console.log("e o menino de papai e");
},1000); //define um tempo de intervalo que vai repetir a mensagem.